# alias-Linux
```
alias up=' sudo apt update && sudo apt list --upgradable && sudo apt upgrade -y && sudo apt autoremove -y &>/dev/null && sudo apt clean -y && sudo apt autoclean -y && sudo clear && echo -e "\n\tDone Boss ! \n"'

```
